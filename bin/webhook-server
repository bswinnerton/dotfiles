#!/usr/bin/env ruby

require 'pp'
require 'json'
require 'sinatra'

[:get, :post, :put, :patch, :delete].each do |method|
  send(method, '*') do
    status 204
    pp JSON.parse(request.body.read)
  end
end

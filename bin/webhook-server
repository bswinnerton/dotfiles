#!/usr/bin/env ruby

require 'pp'
require 'json'
require 'sinatra'

[:get, :post, :put, :patch, :delete].each do |method|
  send(method, '*') do
    body = request.body.read

    if body.empty?
      status 200
      "🖖 🏼"
    else
      status 204
      pp JSON.parse(body)
    end
  end
end

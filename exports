PATH="/usr/local/heroku/bin:$PATH"
PATH="$HOME/.rbenv/bin:$PATH"
PATH="./bin:$PATH"
EDITOR=vim # To run gem open [gem]
if [[ "$OSTYPE" == "darwin"* ]]; then
  SSL_CERT_FILE="/usr/local/etc/openssl/certs/cert.pem" # Fix for http://railsapps.github.com/openssl-certificate-verify-failed.html
fi
